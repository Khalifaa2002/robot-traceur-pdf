"""
control/ — Path Tracking Controllers (v4.0)
=============================================
Responsibilities:
  - PID controller (pid.py)        ← to be moved from core/controller.py (Phase D)
  - Pure Pursuit controller         ← to be created from PythonRobotics (Phase C)
  - Abstract BaseController         ← common interface for all controllers

All controllers must implement: compute(state, target) → (v_cmd, omega_cmd)

Status: SCAFFOLD — not yet populated (Phase B)
"""
